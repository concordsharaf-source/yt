# ملخص التحديث النهائي — جدول الورديات (قناة اليمن اليوم)

## ما تم إنجازه في هذا التحديث

| المشكلة السابقة | الحل المطبق |
|---|---|
| إشعار رسائل جديدة كاذب (العلامة الحمراء تظهر بدون رسائل جديدة) | العدّاد يُحسب من الرسائل الفعلية الجديدة فقط، ويُحدَّث محلياً وفورياً عند فتح الدردشة |
| الرسائل لا تظهر مع رسالة "غير متصل" | صفحة الدردشة أُعيدت هيكلتها بالكامل: الرسائل تظهر محلياً من ذاكرة الجهاز فوراً (مثل واتساب) ثم تُحدَّث من الشبكة |
| تسجيل الخروج عند تحديث الصفحة | الجلسة أصبحت دائمة — لا يخرج المستخدم إلا بالضغط على تسجيل الخروج |
| رسالة التحذير المخيفة في صفحة الإعدادات | حُذفت نهائياً |
| زر "إنهاء جميع الجلسات" | حُذف نهائياً بناءً على طلبك |
| تحذير Tailwind في الكونسول | ملف `tw.css` محلي بدل CDN — لا تحذيرات نهائياً |
| الموقع يعتمد على Google CDN (يتأثر بالحجب) | Firebase SDK (3 ملفات JS) وخط Tajawal (4 ملفات ttf) أصبحت محلية بالكامل |

## الملفات المرفقة (19 ملفاً)

1. `index.html` — الصفحة الرئيسية (جدول الورديات، التقويم، الإحصائيات، الإعدادات)
2. `chat.html` — صفحة الدردشة (بهيكلها الجديد)
3. `config.js` — إعدادات Firebase وكلمات المرور المشفرة
4. `sw.js` + `manifest.json` — إعدادات التطبيق (PWA) والإشعارات
5. `firebase-app.js` + `firebase-database.js` + `firebase-storage.js` — مكتبات Firebase محلية
6. `tw.css` — أنماط التصميم (بديل Tailwind CDN)
7. `fonts.css` + مجلد `fonts/` — خط Tajawal محلي (4 أوزان)
8. مجلد `images/` — أيقونات التطبيق (icon-192x192.png، icon-512x512.png)
9. `database.rules.json` — قواعد أمان Firebase (للاستخدام في Firebase Console فقط)

## طريقة الرفع (مهمة جداً)

1. ارفع **جميع** الملفات والمجلدات (`index.html`, `chat.html`, `config.js`, `sw.js`, `manifest.json`, `tw.css`, `fonts.css`, `firebase-*.js`, مجلد `fonts/`، مجلد `images/`) إلى **نفس المجلد** في الاستضافة.
2. يجب أن تبقى بنية المجلدات كما هي: `fonts/` و `images/` في نفس مستوى `index.html` مباشرة.

## بعد الرفع — مهم جداً لكل مذيع

لكي تظهر التحديثات الجديدة على هواتف المذيعين يجب:

1. فتح إعدادات الهاتف ← التطبيقات ← جدول الورديات ← **مسح البيانات** (أو مسح التخزين المؤقت).
2. أو: فتح الموقع مرة واحدة بالمتصفح بعد الرفع ثم إغلاقه وفتحه من الأيقونة.
3. بدون هذه الخطوة قد يحتفظ الهاتف بالملفات القديمة المخزنة.

## كيفية التحقق من العمل

1. افتح الدردشة: يجب أن تظهر الرسائل القديمة **فوراً** دون انتظار ودون رسالة "غير متصل".
2. أرسل رسالة جديدة من حساب آخر: يجب أن تظهر الإشعارات (شارة حمراء + رقم) فوراً في الصفحة الرئيسية وصفحة الدردشة.
3. حدّث الصفحة (اسحب للأسفل): يجب أن تبقى مسجّلاً داخل حسابك دون خروج.
4. لا يجب أن تظهر أي رسائل تحذير في أدوات المطور (Console).

## ملاحظة أمنية

- كلمات المرور مخزنة مشفرة (SHA-256) ولا توجد نصاً صريحاً في أي ملف.
- فقط حساب **شرف** (الباسورد 232) يستطيع إدارة المستخدمين (إضافة/تعديل/حذف).
- أي تغيير لكلمة المرور يتم التحقق منه في Firebase قبل اعتماده.
